{
  "name": "@ensemble-ai/subgraph",
  "version": "0.1.0",
  "description": "The Graph subgraph for indexing Ensemble Framework events",
  "repository": {
    "type": "git",
    "url": "https://github.com/ensemble-codes/ensemble-framework.git",
    "directory": "packages/subgraph"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "license": "MIT",
  "homepage": "https://github.com/ensemble-codes/ensemble-framework/tree/main/packages/subgraph",
  "bugs": {
    "url": "https://github.com/ensemble-codes/ensemble-framework/issues"
  },
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph codegen && graph build",
    "deploy": "graph deploy ensemble-v2",
    "create-local": "graph create --node http://localhost:8020/ ensemble-hub",
    "remove-local": "graph remove --node http://localhost:8020/ ensemble-hub",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ensemble-hub",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.95.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
