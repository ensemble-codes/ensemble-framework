type Service @entity {
  id: String!
  name: String!
  category: String!
  description: String!
}

type Agent @entity {
  id: Bytes!
  name: String!
  agentUri: String!
  owner: Bytes!
  reputation: BigInt!
  isRegistered: Boolean!
}

type Task @entity {
  id: ID!
  prompt: String!
  issuer: Agent!
  status: String!
  assignee: Bytes
  proposalId: BigInt!
}

type Proposal @entity {
  id: ID!
  issuer: Agent!
  service: String!
  price: BigInt!
}
